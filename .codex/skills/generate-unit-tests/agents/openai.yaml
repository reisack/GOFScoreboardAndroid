interface:
  display_name: "Generate Unit Tests"
  short_description: "Generate branch-covering JVM unit tests"
  default_prompt: "Use $generate-unit-tests and delegate to the workspace tester agent to cover missed branches with local JVM unit tests only."
